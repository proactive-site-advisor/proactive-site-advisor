/**
 * Prefix Configuration
 *
 * After changing values here, run:
 *   npm run build
 */

module.exports = {
    base: 'proactive-site-advisor',          // kebab-case (frontend)
    basePhp: 'proactive_site_advisor',       // snake_case (backend)
    baseConst: 'PROACTIVE_SITE_ADVISOR',     // CONSTANT_CASE
    namespace: 'ProactiveSiteAdvisor',      // JS global namespace
    configObject: 'proactiveSiteAdvisorConfig', // wp_localize_script object
};
